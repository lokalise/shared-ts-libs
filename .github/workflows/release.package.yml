name: Package release to NPM
on:
  workflow_call:
    inputs:
      working_directory:
        required: true
        type: string
      package_name:
        required: true
        type: string

jobs:
  release:
    # Only run for pull requests that has been merged (not closed) and that doesn't have `skip-release` label
    if: github.event.pull_request.merged == true && contains(github.event.pull_request.labels.*.name, 'skip-release') == false
    name: release
    runs-on: ubuntu-latest
    permissions:
      id-token: write  # Required for OIDC trusted publishing
      contents: write  # Required for git operations
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24.x
          scope: '@lokalise'
          registry-url: 'https://registry.npmjs.org'
          cache: 'pnpm'

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Package
        run: pnpm run build --filter=${{ inputs.package_name }}

      - name: Setup git config
        run: |
          git config --global user.email "auto-release@lokalise.com"
          git config --global user.name "AUTO RELEASE"
          pnpm config set git-tag-version false

      - name: Git pull
        run: git pull origin main

      # Apply proper semver according to GitHub labels
      - name: Major label detected
        if: contains(github.event.pull_request.labels.*.name, 'major')
        run: pnpm version major
        working-directory: ${{ inputs.working_directory }}
      - name: Minor label detected
        if: contains(github.event.pull_request.labels.*.name, 'minor')
        run: pnpm version minor
        working-directory: ${{ inputs.working_directory }}
      - name: Patch label detected
        if: contains(github.event.pull_request.labels.*.name, 'patch')
        run: pnpm version patch
        working-directory: ${{ inputs.working_directory }}

      - name: Git push
        run: COMMIT_MSG=$(cd "${{ inputs.working_directory }}" && pnpm run --silent package-version) && git commit -am "${{ inputs.package_name }} $COMMIT_MSG" && git push origin main

      - name: Release Package
        run: pnpm publish --provenance --access public
        working-directory: ${{ inputs.working_directory }}
