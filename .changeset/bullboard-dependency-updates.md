---
"@lokalise/fastify-bullboard-plugin": minor
---

Update dependencies, including `@bull-board/api` and `@bull-board/fastify` to v8. The v8 major only changes the `dateFormats` API (Intl options instead of date-fns strings), which this plugin does not use, so behaviour is unchanged.
