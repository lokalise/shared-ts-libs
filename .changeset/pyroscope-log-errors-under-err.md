---
"@lokalise/pyroscope-profiling": patch
---

Log errors under `err` so pino serializes their message and stack instead of `{}`, and point a failed start on Windows at the README's WSL2 and container routes.
