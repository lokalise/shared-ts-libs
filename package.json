{
  "name": "shared-ts-libs",
  "version": "1.0.0",
  "workspaces": [
    "packages/dev/*",
    "packages/app/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/app/id-utils && npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "lint:fix": "npm run lint:fix --workspaces --if-present"
  },
  "dependencies": {}
}
